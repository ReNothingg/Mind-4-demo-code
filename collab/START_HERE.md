# Обучение Mind-4 в Google Colab

Я подготовил для вас **полное руководство** по обучению вашей Mind-4 модели в Google Colab. Вот что создано:

## Созданные файлы

### 1. **COLAB_QUICK_START.md** (НАЧНИТЕ ОТСЮДА)
- Копируй-вставь код для запуска в Colab
- 5 простых ячеек
- Выбор данных (свой файл, Google Drive, или тестовые)
- Готовые конфигурации для разных GPU

### 2. **GOOGLE_COLAB_TRAINING_GUIDE.md** (Подробная документация)
- Детальное объяснение каждого шага
- Оптимизация для разных GPU
- Решение проблем и FAQ
- Примеры конфигураций

### 3. **colab_train.py** (Основной скрипт)
- Готовый скрипт обучения
- Поддержка чекпоинтов
- Валидация
- Логирование

---

## Самый быстрый старт (3 минуты)

### Шаг 1: Откройте Google Colab
- Перейдите на https://colab.research.google.com
- Создайте новый ноутбук

### Шаг 2: Копируйте-вставляйте (Ячейка 1)

```python
# Установка
!pip install torch pyyaml tqdm -q
!git clone https://github.com/ReNothingg/Mind-4-demo-code.git 2>/dev/null || (cd Mind-4-demo-code && git pull)
%cd Mind-4-demo-code

# Проверка GPU
import torch
print(f"GPU: {torch.cuda.get_device_name(0)}")
```

### Шаг 3: Загрузите данные (Ячейка 2)

**Вариант A - Свой файл:**
```python
from google.colab import files
import shutil

uploaded = files.upload()
shutil.copy(list(uploaded.keys())[0], './train/train_data.txt')
```

**Вариант B - Тестовые данные:**
```python
test_data = """Machine learning is awesome.
""" * 1000

with open('./train/train_data.txt', 'w') as f:
    f.write(test_data)
```

### Шаг 4: Обучение (Ячейка 3)

```python
# Малая конфигурация для T4
!cat > config/train_small.yaml << 'EOF'
model:
  hidden_size: 512
  num_hidden_layers: 8
  num_attention_heads: 8
  num_key_value_heads: 2
  vocab_size: 50000
  head_dim: 64
  intermediate_size: 1024
  num_experts: 4
  experts_per_token: 2
data:
  batch_size: 2
  max_seq_length: 512
training:
  num_epochs: 1
  optimizer:
    lr: 1e-4
EOF

# Запуск обучения
!python colab_train.py --config config/train_small.yaml --data ./train/train_data.txt --dry-run
```

### Шаг 5: Скачайте результаты (Ячейка 4)

```python
from google.colab import files
files.download('./checkpoints/mind_final.ckpt')
```

---

## 🎯 Выбор размера модели

### T4 GPU (бесплатный Colab - ~15 GB память)
```yaml
hidden_size: 512      # легкая модель
num_hidden_layers: 8
batch_size: 2
max_seq_length: 512
```
**Время на 1000 примеров**: ~2 минуты

### P100 GPU (Pro - ~40 GB)
```yaml
hidden_size: 768
num_hidden_layers: 12
batch_size: 4
max_seq_length: 1024
```
**Время**: ~5 минут

### A100 GPU (Premium - ~80 GB)
```yaml
hidden_size: 1024
num_hidden_layers: 16
batch_size: 8
max_seq_length: 2048
```
**Время**: ~3 минуты

---

##  Структура файлов

```
Mind-4-demo-code/
├── COLAB_QUICK_START.md          ← НАЧНИТЕ ОТСЮДА! 🚀
├── GOOGLE_COLAB_TRAINING_GUIDE.md ← Подробный гайд
├── colab_train.py                 ← Основной скрипт обучения
├── config/
│   ├── train.yaml                 ← Полная конфигурация
│   └── train_small.yaml           ← Для Colab (после создания)
├── model/
│   ├── torch/
│   │   ├── model.py              ← Архитектура модели
│   │   └── weights.py            ← Загрузка весов
│   └── generate.py               ← Генерация текста
├── train/
│   └── train_data.txt            ← Ваши обучающие данные
└── checkpoints/                  ← Сохраненные модели
    └── mind_epoch_1.ckpt         ← Итоги обучения
```

---

## 🔧 Что находится в конфиге train.yaml

| Параметр | Значение | Описание |
|----------|----------|----------|
| `hidden_size` | 2880 | Размер скрытого слоя (↑ = лучше, но медленнее) |
| `num_hidden_layers` | 36 | Кол-во слоев (↑ = лучше понимание) |
| `num_attention_heads` | 64 | Кол-во голов внимания |
| `vocab_size` | 50000 | Размер словаря |
| `batch_size` | 8 | Примеры за раз (↓ если OOM) |
| `max_seq_length` | 2048 | Макс длина текста (↓ если OOM) |
| `lr` | 5e-5 | Learning rate (скорость обучения) |
| `num_epochs` | 3 | Количество проходов через данные |

---

## ⚠️ Частые проблемы и решения

### 💾 "CUDA out of memory"

**Решение** - отредактируйте конфиг:
```yaml
batch_size: 1        # вместо 2-8
max_seq_length: 256  # вместо 512-2048
```

### 🔌 "No module named 'mind'"

**Решение** - убедитесь что находитесь в папке:
```python
%cd Mind-4-demo-code  # запустите эту строку
```

### ⏸️ "Colab разорвал соединение"

**Решение** - Colab отключается через 12 часов
- Сохраняйте чекпоинты часто
- Используйте Google Drive для резервной копии:
  ```python
  !cp ./checkpoints/*.ckpt /content/gdrive/My\ Drive/
  ```

### 🐢 "Обучение очень медленное"

**Проверьте:**
```python
import torch
print(torch.cuda.is_available())  # должен быть True
print(torch.cuda.get_device_name(0))  # должен показать GPU
```

---

## 📈 Как отслеживать прогресс

При обучении вы будете видеть:

```
==================================================
Эпоха 1/1
==================================================
Обучение: 100%|██████| 500/500 [3:45<00:00]
Train Loss: 3.2145
==================================================
```

**Loss должен уменьшаться!** Например:
- Шаг 1: Loss = 5.00
- Шаг 10: Loss = 3.50  Хорошо!
- Шаг 20: Loss = 2.10  Отлично!

---

## 🎁 Что дальше?

### После обучения:

1. **Загрузите модель:**
   ```python
   import torch
   checkpoint = torch.load('mind_epoch_1.ckpt')
   model.load_state_dict(checkpoint['model_state_dict'])
   ```

2. **Проверьте генерацию:**
   ```bash
   python model/generate.py ./checkpoints/mind_epoch_1.ckpt --prompt "Hello"
   ```

3. **Улучшайте дальше:**
   - Больше данных = лучше результаты
   - Большее `hidden_size` = более мощная модель
   - Больше `num_hidden_layers` = лучше понимание

---

## Если что-то не работает

**Проверьте в этом порядке:**

1.   GPU включен? → `torch.cuda.is_available()`
2.   В правильной папке? → `%cd Mind-4-demo-code`
3.   Данные загружены? → `ls ./train/train_data.txt`
4.   Конфиг существует? → `cat config/train.yaml`
5.   Памяти достаточно? → уменьшите `batch_size`

---

## Советы для лучших результатов

1. **Качество данных > Размер модели**
   - 1000 хороших примеров > 1M плохих

2. **Начните с маленькой модели**
   - Проверьте что работает прежде чем масштабировать

3. **Сохраняйте регулярно**
   - `save_steps: 100` в конфиге

4. **Экспериментируйте с learning rate**
   - `lr: 5e-5` - стандартное значение
   - `lr: 1e-4` - если модель недообучена
   - `lr: 1e-5` - если loss скачет

5. **Используйте валидацию**
   - Помогает понять переобучение

---

## Вы готовы!

1. Откройте **COLAB_QUICK_START.md**
2. Скопируйте код из ячеек в Colab
3. Запустите
4. Наслаждайтесь! 🚀

**Вопросы? Проблемы? Глюки?** Проверьте FAQ в **GOOGLE_COLAB_TRAINING_GUIDE.md**